arguments:
  - "-f"
  - "/etc/dcgm-exporter/default-counters.csv"

nodeSelector:
  nvidia.com/gpu.present: "true"

tolerations:
  - operator: Exists

service:
  enable: true
  type: ClusterIP
  port: 9400
  address: ":9400"

serviceMonitor:
  enabled: true
  interval: 15s
